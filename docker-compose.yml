version: '3.8'
services:
  omnigent:
    build: .
    ports:
      - "3000:3000"
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - ~/.omnigent:/root/.omnigent
    restart: unless-stopped
